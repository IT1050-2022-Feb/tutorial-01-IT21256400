#include <stdio.h>
int main() {
int Distance=0;
int Amount;
  printf("Enter a Distance: ");
  scanf("%d", &Distance);
  if (Distance < 30) {
     Amount=50*Distance;
       printf("You entered %d.\n", Amount);
   else (Distance < 30) {
Amount=50*Distance;
printf("You entered %d.\n", Amount);
  return 0;
  }
